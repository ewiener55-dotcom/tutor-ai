services:
  - type: web
    name: tutor-ai
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn tutor.asgi:application -k uvicorn.workers.UvicornWorker"
    envVars:
      - key: OPENAI_API_KEY
        sync: false
